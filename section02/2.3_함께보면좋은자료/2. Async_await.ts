/**
 * =========================================================
 * 2. Async / Await + 화살표 함수
 * =========================================================
 *
 * JavaScript와 문법 자체는 거의 동일하다.
 *
 * TypeScript에서는 함수의 매개변수와
 * 반환값에 타입을 지정하는 것이 가장 큰 차이점이다.
 */
